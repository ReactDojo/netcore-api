apiVersion: v1
kind: Service
metadata:
    name: "kubernetescluster-2f2c"
    labels:
        app: "kubernetescluster-2f2c"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kubernetescluster-2f2c"