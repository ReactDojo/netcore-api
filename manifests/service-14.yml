apiVersion: v1
kind: Service
metadata:
    name: "kubeclustersecondlast-b153"
    labels:
        app: "kubeclustersecondlast-b153"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kubeclustersecondlast-b153"