apiVersion: v1
kind: Service
metadata:
    name: "grpckube-978b"
    labels:
        app: "grpckube-978b"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "grpckube-978b"