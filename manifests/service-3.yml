apiVersion: v1
kind: Service
metadata:
    name: "kubernetesonenode-0b68"
    labels:
        app: "kubernetesonenode-0b68"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kubernetesonenode-0b68"