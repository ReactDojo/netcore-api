apiVersion: v1
kind: Service
metadata:
    name: "kubernetesonenode-9f31"
    labels:
        app: "kubernetesonenode-9f31"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kubernetesonenode-9f31"