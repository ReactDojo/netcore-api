apiVersion: v1
kind: Service
metadata:
    name: "kubeclustersecondlast-0101"
    labels:
        app: "kubeclustersecondlast-0101"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kubeclustersecondlast-0101"