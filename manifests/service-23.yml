apiVersion: v1
kind: Service
metadata:
    name: "kubeclusterfifth-e733"
    labels:
        app: "kubeclusterfifth-e733"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kubeclusterfifth-e733"