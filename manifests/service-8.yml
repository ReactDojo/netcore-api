apiVersion: v1
kind: Service
metadata:
    name: "kubernetesclusterlast-fc06"
    labels:
        app: "kubernetesclusterlast-fc06"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kubernetesclusterlast-fc06"