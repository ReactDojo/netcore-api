apiVersion: v1
kind: Service
metadata:
    name: "kubeclusterfifth-ecf0"
    labels:
        app: "kubeclusterfifth-ecf0"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kubeclusterfifth-ecf0"