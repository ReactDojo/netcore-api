apiVersion : apps/v1
kind: Deployment
metadata:
  name: "kubernetesonenode-b4c1"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "kubernetesonenode-b4c1"
  template:
    metadata:
      labels:
        app: "kubernetesonenode-b4c1"
    spec:
      containers:
        - name: "kubernetesonenode-b4c1"
          image: "containerbustrips.azurecr.io/kubernetesonenode"
          ports:
          - containerPort: 80