apiVersion: v1
kind: Service
metadata:
    name: "kubernetescluster-bca6"
    labels:
        app: "kubernetescluster-bca6"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kubernetescluster-bca6"