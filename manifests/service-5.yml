apiVersion: v1
kind: Service
metadata:
    name: "kubernetesonenode-517b"
    labels:
        app: "kubernetesonenode-517b"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kubernetesonenode-517b"